dataset = importdata('signal.txt');
A_dataset = importdata('A_signal.txt');

[b,a] = butter(5,0.2);
dataIn = dataset;
dataOut = filter(b,a,dataIn);
deriv_data = diff()
%fvtool(dataOut);
figure(1);
plot(dataIn);
figure(2);
plot(dataOut);
figure(3);
plot(A_dataset);

